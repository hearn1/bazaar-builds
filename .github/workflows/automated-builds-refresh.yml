# Runs the automated builds refresh pipeline and, in live_cron, updates rolling
# proposal PRs in hearn1/bazaar_tracker.
#
# Required secrets:
# - TRACKER_PR_TOKEN: fine-grained PAT scoped to hearn1/bazaar_tracker with
#   Contents: Read & Write and Pull requests: Read & Write.
# - CLAUDE_API_KEY: Anthropic API key for the LLM classifier in live/LLM mode.

name: Automated builds refresh

permissions:
  contents: write

on:
  schedule:
    - cron: "0 6 * * 0"
  workflow_dispatch:
    inputs:
      hero:
        description: "Hero to refresh"
        required: true
        type: choice
        options:
          - Karnok
          - Mak
          - Dooley
          - Vanessa
          - Pygmalien
          # Jules/Stelle are on hold per ROADMAP; add them when their
          # catalog files land in the tracker repo.
      phase_override:
        description: "Optional one-run phase override: implementation, local_dry_run, shadow_cron, or live_cron"
        required: false
        type: string
      mock_llm:
        description: "Bypass Claude classification for dry-run artifact validation"
        required: false
        default: false
        type: boolean
      classifier_mode:
        description: "Classifier strategy: llm or no_llm_shadow. Scheduled shadow_cron defaults to no_llm_shadow."
        required: false
        default: ""
        type: string

concurrency:
  group: pipeline-${{ github.workflow }}
  cancel-in-progress: false

jobs:
  refresh:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        # Jules/Stelle are on hold per ROADMAP; add them to the matrix when
        # their catalog files land in the tracker repo.
        hero: [Karnok, Mak, Dooley, Vanessa, Pygmalien]

    steps:
      - name: Check selected hero
        id: selected
        run: |
          if [[ "${{ github.event_name }}" != "workflow_dispatch" || "${{ inputs.hero }}" == "${{ matrix.hero }}" ]]; then
            echo "run=true" >> "$GITHUB_OUTPUT"
          else
            echo "run=false" >> "$GITHUB_OUTPUT"
          fi

      - name: Check out bazaar-builds
        if: steps.selected.outputs.run == 'true'
        uses: actions/checkout@v4

      - name: Check out bazaar_tracker
        if: steps.selected.outputs.run == 'true'
        uses: actions/checkout@v4
        with:
          repository: hearn1/bazaar_tracker
          path: bazaar_tracker
          token: ${{ secrets.TRACKER_PR_TOKEN }}

      - name: Set up Python
        if: steps.selected.outputs.run == 'true'
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install dependencies
        if: steps.selected.outputs.run == 'true'
        run: pip install -r requirements.txt

      - name: Install Playwright browsers
        if: steps.selected.outputs.run == 'true'
        run: python -m playwright install --with-deps chromium

      - name: Prepare run state
        if: steps.selected.outputs.run == 'true'
        id: state
        run: |
          python - <<'PY'
          import json
          import os
          from pathlib import Path

          source = Path("pipeline_state.json")
          if source.exists():
              state = json.loads(source.read_text(encoding="utf-8"))
          else:
              state = {
                  "schema_version": 1,
                  "phase": "implementation",
                  "dry_run": True,
                  "patch_label": None,
                  "expected_bazaardb_patch_label": None,
                  "freeze_removals_until": None,
                  "hero_freezes": {},
              }

          override = "${{ inputs.phase_override }}"
          if override:
              allowed = {"implementation", "local_dry_run", "shadow_cron", "live_cron"}
              if override not in allowed:
                  raise SystemExit(f"phase_override must be one of {sorted(allowed)}")
              state["phase"] = override
              state["dry_run"] = override != "live_cron"

          target = Path(os.environ["RUNNER_TEMP"]) / "pipeline_state.json"
          target.write_text(json.dumps(state, indent=2, sort_keys=True) + "\n", encoding="utf-8")
          with Path(os.environ["GITHUB_OUTPUT"]).open("a", encoding="utf-8") as handle:
              handle.write(f"state_file={target}\n")
              handle.write(f"phase={state['phase']}\n")
              handle.write(f"dry_run={str(state['dry_run']).lower()}\n")
          PY

      - name: Configure tracker git identity
        if: steps.selected.outputs.run == 'true' && steps.state.outputs.phase == 'live_cron'
        working-directory: bazaar_tracker
        run: |
          git config user.name "bazaar-builds-bot"
          git config user.email "bazaar-builds-bot@users.noreply.github.com"

      - name: Run pipeline
        if: steps.selected.outputs.run == 'true'
        env:
          CLAUDE_API_KEY: ${{ secrets.CLAUDE_API_KEY }}
          GH_TOKEN: ${{ secrets.TRACKER_PR_TOKEN }}
        run: |
          mock_llm_args=()
          if [[ "${{ inputs.mock_llm }}" == "true" ]]; then
            mock_llm_args+=(--mock-llm)
          fi
          classifier_mode="${{ inputs.classifier_mode }}"
          if [[ -z "$classifier_mode" ]]; then
            if [[ "${{ steps.state.outputs.phase }}" == "shadow_cron" ]]; then
              classifier_mode="no_llm_shadow"
            else
              classifier_mode="llm"
            fi
          fi

          xvfb-run -a python -m automated_builds_pipeline.pipeline run \
            --hero "${{ matrix.hero }}" \
            --state-file "${{ steps.state.outputs.state_file }}" \
            --tracker-repo "$GITHUB_WORKSPACE/bazaar_tracker" \
            --stats-dir "$GITHUB_WORKSPACE/stats" \
            --output-dir "$GITHUB_WORKSPACE/artifacts" \
            --api-key-env CLAUDE_API_KEY \
            --classifier-mode "$classifier_mode" \
            --bazaardb-retries 2 \
            "${mock_llm_args[@]}"

      - name: Persist stats
        if: steps.selected.outputs.run == 'true' && github.ref_name == 'main' && (steps.state.outputs.phase == 'shadow_cron' || steps.state.outputs.phase == 'live_cron')
        run: |
          set -euo pipefail

          git config user.name "bazaar-builds-bot"
          git config user.email "bazaar-builds-bot@users.noreply.github.com"

          stats_file="stats/$(echo "${{ matrix.hero }}" | tr '[:upper:]' '[:lower:]' | sed -E 's/[^a-z0-9]+/_/g; s/^_//; s/_$//')_stats.json"
          if [[ ! -f "$stats_file" ]]; then
            echo "No stats file was written for ${{ matrix.hero }}"
            exit 0
          fi

          git add "$stats_file"
          if git diff --cached --quiet -- "$stats_file"; then
            echo "No stats changes for ${{ matrix.hero }}"
            exit 0
          fi

          git commit -m "automated: persist ${{ matrix.hero }} stats"

          for attempt in 1 2 3; do
            git pull --rebase --autostash origin main
            if git push origin main; then
              exit 0
            fi
            echo "Stats push attempt ${attempt} failed"
          done

          echo "Failed to persist stats after 3 attempts"
          exit 1

      - name: Upload shadow artifacts
        if: steps.selected.outputs.run == 'true' && (steps.state.outputs.phase != 'live_cron' || steps.state.outputs.dry_run == 'true')
        uses: actions/upload-artifact@v4
        with:
          name: automated-builds-${{ matrix.hero }}-${{ github.run_id }}
          path: artifacts/
          if-no-files-found: ignore
