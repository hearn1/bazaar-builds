# Marks the artifacts/ directory as a Python package so that tracked scripts
# (summarize_diffs.py, verify_apply.py, sweep_dry_run.py) can be imported by
# the test suite without sys.path manipulation.
